#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,m,a,x;
    cin >> n >> m >> a;
    for(int i=0;i<n;i++){
        cin >> x;
    }
    for(int i=0;i<a;i++){
        cin >> x;
    }
}